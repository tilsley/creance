{
  "name": "@agent-os/credential-broker-demo",
  "private": true,
  "type": "module",
  "description": "Demonstrates the CredentialBroker: an agent acts on an authenticated API with a scoped credential it never sees (ADR-0010)",
  "scripts": {
    "start": "bun run index.ts"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*"
  }
}
