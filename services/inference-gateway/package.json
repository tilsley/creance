{
  "name": "@agent-os/inference-gateway",
  "private": true,
  "type": "module",
  "description": "The privileged inference choke point — POST /v1/generate (bespoke) + /v1/messages (Anthropic wire, ADR-0028); holds model creds + budget admission, callers hold none (ADR-0019)",
  "scripts": {
    "start": "bun run server.ts"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*",
    "@aws-sdk/client-bedrock-runtime": "^3.716.0"
  }
}
