node_modules
**/node_modules
.git
**/cdk.out
**/.cdk.staging
**/*.output
*.log
.env
.env.*
# Python venvs/caches — the retired LiteLLM reference's .venv is ~440MB and is needed by no
# image (it's the conformance reference, run via uv on the host). Excluding it slims EVERY build.
**/.venv
**/__pycache__
**/*.pyc
# Claude Code session state (worktrees hold a full stale repo copy) — never ship it
.claude
# runtime images need no app (console ships via BucketDeployment; doc-gardener is not an image)
apps/
