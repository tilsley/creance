# dependencies
node_modules/
bun.lockb

# CDK
infra/cdk.out/
infra/.cdk.staging/
*.js
*.d.ts
!**/*.config.js

# env / secrets
.env
.env.*
*.pem

# agent runtime memory (files-first `remember`, ADR-0030) — durable per-run state, not source
**/.memory/
**/.agent-memory/

# OS / editor
.DS_Store
.idea/
.vscode/
