# dependencies
node_modules/
bun.lockb

# CDK
infra/cdk.out/
infra/.cdk.staging/

# console (ADR-0032): built bundle + per-dev runtime config (deployed one is CDK-written)
apps/console/dist/
apps/console/public/config.json
*.js
*.d.ts
!**/*.config.js

# env / secrets
.env
.env.*
*.pem

# agent runtime memory (files-first `remember`, ADR-0030) — durable per-run state, not source
**/.memory/
**/.agent-memory/

# OS / editor
.DS_Store
.idea/
.vscode/
__pycache__/
