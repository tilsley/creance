# Architecture Decision Records

Each ADR captures one significant decision: its context, the choice, and the
consequences. Format follows Michael Nygard's template. ADRs are immutable once
**Accepted** — to change a decision, add a new ADR that supersedes the old one.

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-build-on-eks-karpenter.md) | Build the sandbox layer on EKS + Karpenter | Accepted (amended by 0006) |
| [0002](0002-gvisor-as-default-untrusted-tier.md) | gVisor as the default untrusted-code tier | Superseded by 0006 |
| [0003](0003-ports-and-adapters.md) | Ports & adapters for environment portability (local ↔ EKS) | Accepted |
| [0004](0004-cost-governance.md) | Cost governance — attribution vs. enforcement | Accepted |
| [0005](0005-crossplane-control-plane.md) | Crossplane as the self-service provisioning control plane | Accepted |
| [0006](0006-agentcore-execution-environment.md) | AgentCore as execution environment; k8s as control plane | Accepted |
| [0007](0007-tools-and-external-auth.md) | Tools & external auth — AgentCore Gateway + Identity, behind ports | Accepted |
| [0008](0008-guard-content-safety-primitive.md) | Guard (content safety) is a third cross-cutting control | Accepted |
| [0009](0009-gate-identity-and-governance.md) | Gate (identity & governance) — port + thin local adapter, managed swap-ins | Accepted |
| [0010](0010-credential-broker.md) | CredentialBroker — thin local broker + authenticated tool (implements 0007) | Accepted |
| [0011](0011-tool-mcp-gateway.md) | Tool/MCP gateway — client-side ToolProvider; AgentCore Gateway as swap-in (implements 0007) | Accepted |
| [0012](0012-agent-control-plane.md) | Agent control plane — registry (catalog) + controller (reconciler) via an Agent CRD | Accepted |
| [0013](0013-inference-cost-enforcement.md) | Inference cost enforcement — buy the gateway (LiteLLM behind the port), own the worst-case admission policy (refines 0004) | Proposed (engine half reversed by 0028; admission policy carries over) |
| [0014](0014-per-tenant-workload-identity.md) | Per-tenant workload identity — assume-role + Budget Action enforcement (implements 0009, refines 0013) | Proposed |
| [0015](0015-split-authn-authz-ports.md) | Split identity (authn) + policy (authz) into separate swappable ports — mesh-trust + OPA seam (refines 0009) | Proposed |
| [0016](0016-obo-token-vault.md) | On-behalf-of credentials via a token-exchange vault (RFC 8693) — act AS the user; per-target service-account vs OBO (realizes 0010) | Proposed |
| [0017](0017-a2a-identity-propagation.md) | Agent-to-agent identity propagation — the OBO delegation chain (nested act) flows through the gate at every hop (extends 0016) | Proposed |
| [0018](0018-a2a-protocol-transport.md) | Adopt the A2A (Agent2Agent) protocol — Agent Card discovery + JSON-RPC message/send/tasks/get for interoperable agent-to-agent calls (supersedes 0017's bespoke hop) | Proposed |
| [0019](0019-inference-gateway.md) | Promote the inference gateway to a standalone, identity-bound choke point — verified workload identity (OIDC), claim-bound isolation, multi-scope atomic budget, CRD self-service (extends 0004/0013, hardens 0014/0015) | Accepted |
| [0020](0020-sandbox-execution-model.md) | Sandbox execution model — tool-executor (A) + sandboxed-agent (B); gateway governs think, sandbox governs do; gateway-gated egress; E2B/AgentCore/local adapters | Accepted |
| [0021](0021-inference-onboarding-policy.md) | Inference onboarding is policy, not provisioning — a tenant asserts an InferenceClaim (CRD, CEL-validated, controller-free) read via the ClaimSource port; per-tenant AWS becomes opt-in (supersedes the provisioning assumption of 0014/0005) | Proposed |
| [0022](0022-sandbox-backends-for-coding-agents.md) | Sandbox backends for coding agents — pick the SandboxProvider adapter by profile (AgentCore Code Interpreter for Model A; E2B / self-hosted-k8s for Model B); AgentCore's 3 network modes; egress lockdown is the load-bearing safety control (refines 0020/0006) | Proposed |
| [0023](0023-memory-backends-postgres-redis.md) | Memory backends — Postgres (system of record: RunStore, SpendStore, long-term + pgvector) + Redis (hot tier: cache, queue, locks), not DynamoDB; new adapters behind existing ports (realizes the remember primitive) | Proposed |
| [0024](0024-build-vs-buy-managed-agent-platforms.md) | Build vs buy — managed agent platforms (AgentCore/Claude MA/Vertex) don't meet R2 (real-time budget) + R6 (portability); use them as adapters behind the ports and keep the thin governance shell; cost is a curve (managed at POC, self-host at scale) — see costs.md | Proposed (buy-the-engine reversed for the gateway by 0028; managed-as-adapter + cost curve stand) |
| [0025](0025-cost-allocation-in-the-ledger.md) | Cost allocation lives in the gateway's ledger, not per-tenant Bedrock profiles — model teams/services/users as software buckets (LiteLLM keys + SpendStore) behind one shared IAM footprint; tagged inference profiles become opt-in for AWS-invoice chargeback only (refines 0004's attribution half; walks back the per-tenant-profile assumption of 0013/0021) | Proposed |
| [0026](0026-gateway-hot-path-authn-authz-budget.md) | Gateway hot-path — no serial remote round-trip per request: authn at the edge (Istio mesh-trust / offline JWT vs cached JWKS), grants cached in-process/Redis or via OPA bundle, worst-case budget reserve as a durable Postgres conditional UPDATE (Redis for grant cache + rate-limit, not the money); built in LiteLLM's OSS hooks (its JWT authn is an enterprise upsell we decline; worst-case R2 is unbuyable at any tier) — realizes 0023's hot tier, refines 0019/0024 | Proposed (rules carry over to 0028's owned engine; LiteLLM-hook impl retired) |
| [0027](0027-two-deployment-profiles.md) | Two deployment profiles — cheap AWS-native (Bun gateway, DynamoDB, scale-to-zero) vs full-k8s (LiteLLM + Redis/Postgres + mesh + OPA), selected by env bundle; one contract with R1 (verified identity) + R2 (real-time budget) invariant across both; a shared conformance suite keeps the two gateways from drifting (realizes 0024's cost curve via the M4 wire switch) | Accepted (amended by 0028 — two profiles, one gateway) |
| [0028](0028-own-the-gateway-engine.md) | Own the gateway engine — retire LiteLLM from the serving path; one Bun/TS gateway in both profiles (Anthropic wire via InvokeModelWithResponseStream passthrough + SSE re-framing, Converse behind the neutral port, OpenAI wire deferred to ConverseStream); default-deny + streamed pop-once settle ported from the hook findings; conformance suite gates the migration (reverses the buy-the-engine half of 0013/0024/0026, revises 0027) | Accepted (executed 2026-06-13 — LiteLLM retired from the serving path) |
| [0029](0029-governed-egress-choke-points.md) | Governed egress — every agent outbound flows through a choke point that (a) holds the credential and (b) bounds the capability: think/inference-gateway, do-net/proxy+wall, do-tools/MCP-gateway, do-http/guarded-fetch; none a new primitive (controls/adapters on think/do). Two threats: cred theft (solved by no/short creds) vs authorized misuse (bounded, not solved). `remember` considered for a gateway and declined — keyless creds + a typed non-SQL port + egress containment suffice; the centralized MCP gateway is the one choke point still owed as a service | Accepted (the owed MCP gateway is built, in-cluster-proven + chart-integrated) |
| [0030](0030-memory-model.md) | Memory model — `remember` is a primitive behind a port, not a chosen storage shape; three tiers (working/episodic/semantic), per-tenant-isolated + append-mostly, with files-first (transparent, sandbox-governed; the coding/cheap default — structural/agentic search wins for code, vectors additive/hybrid) and vector/graph (Mem0/Zep; the recall/scale default) as co-equal adapters; retrieval not storage is the hard part; repositions 0023 (Postgres/pgvector → durable backing + index, not interface), settles 0029's access-policy question | Accepted (+ 2026-06-24 research refinement) |
| [0031](0031-serverless-substrate-for-the-run-loop.md) | Serverless substrate for the run loop — the agent loop runs as a Fargate task-per-run behind a Lambda Function URL → router Lambda, reusing the existing non-k8s adapter bundle (no new ports); Lambda's 15-min cap rules it out for the loop but fits the front door; state on DynamoDB on-demand, watch via run-store polling (SSE/bus deferred until token-level UX is required); realizes the "serverless / scale-to-zero" compute cell 0027 named but left unrealized for the runtime | Accepted (deployed + verified live 2026-07-04) |
| [0032](0032-web-console-cognito.md) | Web console behind Cognito — a static SPA (S3+CloudFront) drives the serverless front door; human identity enters as a `cognito-jwt` Authenticator adapter (offline JWKS verify, id token, `custom:tenant` claim), so the website's login token IS the identity the gate verifies (R1/R2 unchanged); watch = run-store polling per 0031; the operator console (one-click EKS) is deferred to its own ADR (realizes the human-IdP leg of 0015; consumes 0031; cheap profile of 0027) | Accepted (deployed + verified live 2026-07-05) |
| [0033](0033-claude-code-hosted-runner.md) | Claude Code as a hosted runner — headless `claude -p` as a second Fargate executor behind 0031's dispatch seam, selected by `AgentSpec.kind: "claude-code"` (one PutItem, no redeploy); stream-json events mirrored into the run's messages so the 0032 console watches it unchanged; auth = the operator's Max-subscription token (`claude setup-token`) as an SSM SecureString under the default key ($0), with keyless Bedrock (`CLAUDE_CODE_USE_BEDROCK=1` + task role) as the documented multi-tenant graduation path; container = the permission boundary (non-root `bypassPermissions`, `--max-turns`, hard timeout) (consumes 0031/0032; third execution kind after 0019) | Proposed |
| [0034](0034-egress-sidecar-credential-injection.md) | Credential-injecting egress sidecar — the claude-code task gains an `egress-sidecar` container that alone holds the GitHub PAT (SSM SecureString, agent container secret-free for git) and proxies git smart-HTTP on localhost with two capability bounds: per-run repo allowlist pinned to the gate-authorized `Run.repo` (caller-chosen resource, authz-decided via `attributes` on the Authorizer port; agents stay repo-agnostic — 2026-07-05 refinement) and a pkt-line-parsed push policy (refs/heads/run/* creates/updates only — no default-branch writes, deletes, or tags); shim clones through it pre-run and pushes run/<id> post-run (even on crash); microsandbox-style secrets-never-enter-the-agent via endpoint remapping, no TLS interception; inference token behind the same choke point deferred pending an ANTHROPIC_BASE_URL live test (realizes 0029's do-net cell for 0033's runner) | Proposed |
| [0035](0035-record-backend-grafana-cloud.md) | Record backend for the cheap profile — the loop's existing spans (agent.run/turn/tool/guard, already instrumented) export OTLP **direct to Grafana Cloud** (free tier, no collector), auth via SSM SecureString, endpoint via CDK context; fixes the RECORD/TELEMETRY env mismatch that silently disabled spans; ADOT→OpenSearch (DataLogStack) stays the documented full-profile shape (fills 0027's cheap record cell; console 0032 = tenant view, this = operator view) | Accepted (deployed + verified live 2026-07-05) |
| [0036](0036-foreign-l1-boundary-governance.md) | The foreign-L1 agent — names the governance profile the claude-code runner (0033) quietly introduced: an L2 agent that brings its OWN L1 (Claude Code *is* the loop), so its think/do never traverse the in-loop choke points (inference gateway per-turn, sandbox). Governance therefore moves to the two boundaries the platform owns — the gate (R1: identity + authz + quota, admission-time) and the egress sidecar (0034: credentials held, capabilities bounded) — coarse, not per-turn, by construction (you don't own a foreign loop). R2 is re-expressed as an **admission quota** (runs/concurrency), not a dollar cap, because subscription cost is $0; `costUsd` becomes attribution, not enforcement. Names the forced either/or: $0 + boundary-only governance (subscription→provider) vs in-loop governed think (subscription dropped → Bedrock via gateway, per-token) — the sidecar `/v1/*` target is the dial. Third execution model beside 0019/0020's loop-executor + sandboxed-agent | Proposed |
| [0037](0037-hosted-claude-code-landscape.md) | Hosted headless Claude Code — landscape & positioning (mid-2026 survey): our Fargate task-per-run = Anthropic's official "Ephemeral" hosting pattern; our egress sidecar = the field's converged credential-isolation standard (GitHub's own gh-aw-firewall, Cloudflare, Microsandbox, Anthropic's hosted git proxy all independently ship it), and tighter (path/ref bounds vs host allowlist); microVM-per-run **ahead** of OpenHands/Devin's shared-kernel Docker; run-count quotas idiomatic (Devin ACUs, Cursor). Decision: don't rebuild, adopt three field-flagged upgrades — GitHub App per-run tokens (retire the PAT), per-run loop detector (inner bound pairing the quota), optional SessionStore resume — and fix the auth posture: subscription/setup-token is fine for single-human use incl. a sole-contributor org (ToS line = *other humans' runs*, not personal-vs-org); second human → Bedrock (keep the 0036 dial warm); watch the paused-but-roadmapped programmatic billing split | Proposed |
| [0038](0038-agent-onboarding-behind-the-gate.md) | Agent onboarding behind the gate — registry writes go through the platform API (`POST /agents` / `DELETE /agents/{name}`: authn, `agent:register` authz with kind attributes, handler validation, tenant STAMPED from verified identity), GitOps repos become clients of the API (register script = login + POST, zero AWS creds) not a second write path to the table; kube registry stays kubectl-only (the real API server); agents-cli demoted to operator break-glass — the lesson: the CRD was never the point, the API server was (extends 0012 the way 0021 did claims; rides 0032 identity + 0034 attribute authz) | Proposed |
| [0039](0039-gateway-on-the-serverless-substrate.md) | The inference gateway on the serverless substrate — the Bun gateway as a scale-to-zero Lambda + Function URL (0031's packaging applied to 0019's choke point): foreign-agent kinds (sandboxed/custom) get a governed think-path (Cognito-verified run token, reserve→invoke→settle on the shared budgets ledger, callers hold no model creds); NEW `AGENT_GATEWAY_URL` env = "where delegated agents think" so the loop's direct-Bedrock think is untouched — routing ALL think through the gateway is a documented one-env-var graduation, not a side effect | Proposed |
| [0040](0040-coded-agents-services-vs-libraries.md) | Running coded agents — trust decides service vs library: control points (gateway, front door, ledger, registration, sidecar) stay hosted because enforcement-as-a-library is enforcement foreign code can skip; the client side (ports, gateway client, telemetry, even the L1 loop) is publishable as `@agent-os/client`. Names the **external agent** mode: own infra + SDK → governed think through 0039's gateway, ungoverned act (mesh's job), and NO Run resource — *the Run exists only where the platform owns execution*. Staged: (1) extract the client SDK, failure-analyst adopts in place — forces 0038's machine-identity seam (a ~1h human Cognito token can't authenticate a service); (2) the custom kind (per-run Fargate, agent image, sidecar seat) whose ADR must decide image provenance, task-def lifecycle (`POST /agents` creates ECS task defs — first real control-plane op), and the run contract | Proposed |
| [0041](0041-machine-identity-cognito-m2m.md) | Machine identity — confidential app clients on the SAME pool via OAuth2 client_credentials (0038's seam closed): subject = `client_id`, tenant = a resource-server scope grant (`agent-os/tenant.<t>` — the machine analog of custom:tenant, since Cognito stamps neither aud nor custom claims on M2M tokens); `AUTHN=cognito` becomes `composite(cognito-jwt, cognito-m2m)` so front door + gateway admit both credential kinds by redeploy. Secret-but-scoped accepted plainly (revocable per-service client, never platform-stored); SigV4 authenticator = the keyless graduation for callers with AWS identity. SDK: `machineTokenProvider` (cache + refresh) outlives the human 1h ceiling. First subject: `svc-failure-analyst` | Proposed |
| [0042](0042-agentcore-managed-profile.md) | The managed AgentCore profile — lean-in as a THIRD named deployment profile beside cheap AWS-native + full-k8s (extends 0027), selected by env bundle: loop on AgentCore Runtime (our container, rung 1 — never Harness) via `DISPATCH=agentcore` on 0031's seam; tools = AgentCore Gateway over `MCP_SERVERS` + Identity token vault (the 0011 swap + owed 3LO); memory = managed adapter with IAM-enforced per-tenant namespaces (0030's reserved seat); Cognito re-used as the Runtime/Gateway JWT authorizer (one identity plane, 0032/0041). Invariant shell unchanged in all three profiles: gate, inference gateway, router/front door, claims — and the claude-code/foreign-L1 lane stays Fargate everywhere (2vCPU/8GB, no sidecar seat, no domain egress — comparison §15). Supersedes 0006's hosting stance for this profile only. Active-CPU billing makes managed plausibly CHEAPER than the cheap profile at POC utilization | Proposed |
| [0043](0043-spec-first-api-edge.md) | The spec-first edge — each public surface keeps a PURE OpenAPI contract beside its code (no x-amazon-* extensions, so one file describes pod AND Lambda profiles); `SpecRestApiEdge` injects the aws_proxy overlay at synth from the real functionArn (janey's pattern minus the logical-ID/Fn::Sub coupling, CORS-mock bloat, and failOnWarnings:false), giving API Gateway REST APIs on `api./inference.creance.nathantilsley.com` with edge body-validation; Function URLs retired (no raw URL exists), CORS moved into the app (withCors — identical on every substrate), SDK wire types now ALIASES of the generated contract types, and an in-process contract test walks the spec against the real app. Streaming graduation = a STREAM transfer-mode flag (APIGW REST streams since Nov 2025 — the 0039 caveat expired) | Accepted |
