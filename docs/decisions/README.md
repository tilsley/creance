# Architecture Decision Records

Each ADR captures one significant decision: its context, the choice, and the
consequences. Format follows Michael Nygard's template. ADRs are immutable once
**Accepted** — to change a decision, add a new ADR that supersedes the old one.

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-build-on-eks-karpenter.md) | Build the sandbox layer on EKS + Karpenter | Accepted (amended by 0006) |
| [0002](0002-gvisor-as-default-untrusted-tier.md) | gVisor as the default untrusted-code tier | Superseded by 0006 |
| [0003](0003-ports-and-adapters.md) | Ports & adapters for environment portability (local ↔ EKS) | Accepted |
| [0004](0004-cost-governance.md) | Cost governance — attribution vs. enforcement | Accepted |
| [0005](0005-crossplane-control-plane.md) | Crossplane as the self-service provisioning control plane | Accepted |
| [0006](0006-agentcore-execution-environment.md) | AgentCore as execution environment; k8s as control plane | Accepted |
| [0007](0007-tools-and-external-auth.md) | Tools & external auth — AgentCore Gateway + Identity, behind ports | Accepted |
| [0008](0008-guard-content-safety-primitive.md) | Guard (content safety) is a third cross-cutting control | Accepted |
| [0009](0009-gate-identity-and-governance.md) | Gate (identity & governance) — port + thin local adapter, managed swap-ins | Accepted |
| [0010](0010-credential-broker.md) | CredentialBroker — thin local broker + authenticated tool (implements 0007) | Accepted |
| [0011](0011-tool-mcp-gateway.md) | Tool/MCP gateway — client-side ToolProvider; AgentCore Gateway as swap-in (implements 0007) | Accepted |
| [0012](0012-agent-control-plane.md) | Agent control plane — registry (catalog) + controller (reconciler) via an Agent CRD | Accepted |
| [0013](0013-inference-cost-enforcement.md) | Inference cost enforcement — buy the gateway (LiteLLM behind the port), own the worst-case admission policy (refines 0004) | Proposed (engine half reversed by 0028; admission policy carries over) |
| [0014](0014-per-tenant-workload-identity.md) | Per-tenant workload identity — assume-role + Budget Action enforcement (implements 0009, refines 0013) | Proposed |
| [0015](0015-split-authn-authz-ports.md) | Split identity (authn) + policy (authz) into separate swappable ports — mesh-trust + OPA seam (refines 0009) | Proposed |
| [0016](0016-obo-token-vault.md) | On-behalf-of credentials via a token-exchange vault (RFC 8693) — act AS the user; per-target service-account vs OBO (realizes 0010) | Proposed |
| [0017](0017-a2a-identity-propagation.md) | Agent-to-agent identity propagation — the OBO delegation chain (nested act) flows through the gate at every hop (extends 0016) | Proposed |
| [0018](0018-a2a-protocol-transport.md) | Adopt the A2A (Agent2Agent) protocol — Agent Card discovery + JSON-RPC message/send/tasks/get for interoperable agent-to-agent calls (supersedes 0017's bespoke hop) | Proposed |
| [0019](0019-inference-gateway.md) | Promote the inference gateway to a standalone, identity-bound choke point — verified workload identity (OIDC), claim-bound isolation, multi-scope atomic budget, CRD self-service (extends 0004/0013, hardens 0014/0015) | Accepted |
| [0020](0020-sandbox-execution-model.md) | Sandbox execution model — tool-executor (A) + sandboxed-agent (B); gateway governs think, sandbox governs do; gateway-gated egress; E2B/AgentCore/local adapters | Accepted |
| [0021](0021-inference-onboarding-policy.md) | Inference onboarding is policy, not provisioning — a tenant asserts an InferenceClaim (CRD, CEL-validated, controller-free) read via the ClaimSource port; per-tenant AWS becomes opt-in (supersedes the provisioning assumption of 0014/0005) | Proposed |
| [0022](0022-sandbox-backends-for-coding-agents.md) | Sandbox backends for coding agents — pick the SandboxProvider adapter by profile (AgentCore Code Interpreter for Model A; E2B / self-hosted-k8s for Model B); AgentCore's 3 network modes; egress lockdown is the load-bearing safety control (refines 0020/0006) | Proposed |
| [0023](0023-memory-backends-postgres-redis.md) | Memory backends — Postgres (system of record: RunStore, SpendStore, long-term + pgvector) + Redis (hot tier: cache, queue, locks), not DynamoDB; new adapters behind existing ports (realizes the remember primitive) | Proposed |
| [0024](0024-build-vs-buy-managed-agent-platforms.md) | Build vs buy — managed agent platforms (AgentCore/Claude MA/Vertex) don't meet R2 (real-time budget) + R6 (portability); use them as adapters behind the ports and keep the thin governance shell; cost is a curve (managed at POC, self-host at scale) — see costs.md | Proposed (buy-the-engine reversed for the gateway by 0028; managed-as-adapter + cost curve stand) |
| [0025](0025-cost-allocation-in-the-ledger.md) | Cost allocation lives in the gateway's ledger, not per-tenant Bedrock profiles — model teams/services/users as software buckets (LiteLLM keys + SpendStore) behind one shared IAM footprint; tagged inference profiles become opt-in for AWS-invoice chargeback only (refines 0004's attribution half; walks back the per-tenant-profile assumption of 0013/0021) | Proposed |
| [0026](0026-gateway-hot-path-authn-authz-budget.md) | Gateway hot-path — no serial remote round-trip per request: authn at the edge (Istio mesh-trust / offline JWT vs cached JWKS), grants cached in-process/Redis or via OPA bundle, worst-case budget reserve as a durable Postgres conditional UPDATE (Redis for grant cache + rate-limit, not the money); built in LiteLLM's OSS hooks (its JWT authn is an enterprise upsell we decline; worst-case R2 is unbuyable at any tier) — realizes 0023's hot tier, refines 0019/0024 | Proposed (rules carry over to 0028's owned engine; LiteLLM-hook impl retired) |
| [0027](0027-two-deployment-profiles.md) | Two deployment profiles — cheap AWS-native (Bun gateway, DynamoDB, scale-to-zero) vs full-k8s (LiteLLM + Redis/Postgres + mesh + OPA), selected by env bundle; one contract with R1 (verified identity) + R2 (real-time budget) invariant across both; a shared conformance suite keeps the two gateways from drifting (realizes 0024's cost curve via the M4 wire switch) | Accepted (amended by 0028 — two profiles, one gateway) |
| [0028](0028-own-the-gateway-engine.md) | Own the gateway engine — retire LiteLLM from the serving path; one Bun/TS gateway in both profiles (Anthropic wire via InvokeModelWithResponseStream passthrough + SSE re-framing, Converse behind the neutral port, OpenAI wire deferred to ConverseStream); default-deny + streamed pop-once settle ported from the hook findings; conformance suite gates the migration (reverses the buy-the-engine half of 0013/0024/0026, revises 0027) | Accepted (executed 2026-06-13 — LiteLLM retired from the serving path) |
| [0029](0029-governed-egress-choke-points.md) | Governed egress — every agent outbound flows through a choke point that (a) holds the credential and (b) bounds the capability: think/inference-gateway, do-net/proxy+wall, do-tools/MCP-gateway, do-http/guarded-fetch; none a new primitive (controls/adapters on think/do). Two threats: cred theft (solved by no/short creds) vs authorized misuse (bounded, not solved). `remember` considered for a gateway and declined — keyless creds + a typed non-SQL port + egress containment suffice; the centralized MCP gateway is the one choke point still owed as a service | Accepted (the owed MCP gateway is built, in-cluster-proven + chart-integrated) |
| [0030](0030-memory-model.md) | Memory model — `remember` is a primitive behind a port, not a chosen storage shape; three tiers (working/episodic/semantic), per-tenant-isolated + append-mostly, with files-first (transparent, sandbox-governed; the coding/cheap default — structural/agentic search wins for code, vectors additive/hybrid) and vector/graph (Mem0/Zep; the recall/scale default) as co-equal adapters; retrieval not storage is the hard part; repositions 0023 (Postgres/pgvector → durable backing + index, not interface), settles 0029's access-policy question | Accepted (+ 2026-06-24 research refinement) |
| [0031](0031-serverless-substrate-for-the-run-loop.md) | Serverless substrate for the run loop — the agent loop runs as a Fargate task-per-run behind a Lambda Function URL → router Lambda, reusing the existing non-k8s adapter bundle (no new ports); Lambda's 15-min cap rules it out for the loop but fits the front door; state on DynamoDB on-demand, watch via run-store polling (SSE/bus deferred until token-level UX is required); realizes the "serverless / scale-to-zero" compute cell 0027 named but left unrealized for the runtime | Accepted (deployed + verified live 2026-07-04) |
